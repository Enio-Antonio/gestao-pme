#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../uuid/dist/esm/bin/uuid" "$@"
else
  exec node  "$basedir/../../../../uuid/dist/esm/bin/uuid" "$@"
fi
# cmd-shim-target=/home/franciscojs/Desktop/Public/gestao-pme/node_modules/.pnpm/@lumi.new+sdk@0.4.3/node_modules/uuid/dist/esm/bin/uuid
